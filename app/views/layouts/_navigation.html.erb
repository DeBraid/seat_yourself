<div class="navbar row nav1">
      <a class="toggle" gumby-trigger="#example-nav &gt; .nav1 &gt; ul" href="#"><i class="icon-menu" title=".icon-menu"></i></a>
      <h1 class="four columns logo">
        <%= link_to image_tag("my_restaurant.png", :class => "logo_height"), root_path %>
        </a>
      </h1>
      <ul class="eight columns">
        <li>
        	<%= link_to("Create a Restaurant ", new_restaurant_path) %>
        </li>
        <li>
        <li>
          <%= link_to("Sign Up ", new_user_path) %>
        </li>
          <% if current_user.nil? %>
          <%= link_to("Log In", "/login") %>
          <% else %>
          <li style="color:yellow;"><%= link_to(current_user.email, current_user) %></li>
          <%= link_to("Log Out", session_path("current"), :method => :delete ) %>
          <% end %>
      </ul>
    </div>
